#version 460 core

#include "commands.glsl"

layout (local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

DECLARE_COMMANDS(writeonly);

void main() {
    commands[gl_GlobalInvocationID.x].instanceCount = 0;
}
